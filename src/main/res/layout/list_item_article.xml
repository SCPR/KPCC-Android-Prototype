<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="150dp">

    <ImageView
        android:id="@+id/article_list_item_asset_ImageView"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:layout_alignParentBottom="true"
        android:scaleType="centerCrop"
        android:contentDescription="@string/placeholder_asset_description" />

    <TextView
        android:id="@+id/article_list_item_title_textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/transparent_black"
        android:textStyle="bold"
        android:padding="4dp"
        android:textColor="@android:color/white"
        android:text="@string/placeholder_article_short_title" />

</RelativeLayout>
